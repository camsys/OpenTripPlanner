package org.opentripplanner.routing.core;

/**
 * Set a hint to be interpreted per configuration as
 * to what parameters to set to optimize per these settings.
 */
public enum OptimizeHint {
    QUICK,
    WALKING,
    TRANSFERS
}
